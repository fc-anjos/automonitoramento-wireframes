{
  "name": "automonitoramento-wireframes",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Wireframes do Sistema de Automonitoramento (UGRHI-07 / Baixada Santista) — React",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:v2": "npm ci --prefix wireframe-2 && npm run build --prefix wireframe-2 && rm -rf dist/v2 && mkdir -p dist/v2 && cp -R wireframe-2/dist/. dist/v2/",
    "build:all": "npm run build && npm run build:v2",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
